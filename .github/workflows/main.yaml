name: Deploy WebApp
on:
  push:
    branches:
      - master
  workflow_dispatch:
    branches:
      - master

env:
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Echo Build on Master
        run: echo 'Build on master'


  deploy:
    needs: build
    name: Deploy to production
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: echo 'Deploy to production'